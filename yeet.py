from zellegraphics import *
from math import *
windowLength= 500
windowWidth= 500
ORIGIN= Point(250,250)
win = GraphWin("requested graph" , windowLength, windowWidth)
axis = Line(Point(0,windowLength/2), Point(500,windowLength/2))
axis2 = Line(Point(windowLength/2,0), Point(windowLength/2, 500))
axis.draw(win)
axis2.draw(win)
 def graphMaker(equation):
     for x in range(0,equation.len) 
def convertToFunction(eqString):
    pythonString=
    if eqString

win.getMouse()
win.close() 
